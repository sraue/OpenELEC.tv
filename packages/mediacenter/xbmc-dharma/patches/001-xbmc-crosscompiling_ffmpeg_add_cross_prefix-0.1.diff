diff -Naur xbmc-dharma-35100/configure.in xbmc-dharma-35100.patch/configure.in
--- xbmc-dharma-35100/configure.in	2010-10-30 05:39:02.000000000 +0200
+++ xbmc-dharma-35100.patch/configure.in	2010-11-01 04:24:45.975175652 +0100
@@ -1635,6 +1635,7 @@
       --disable-static \
       `if test "$use_debug" = "no"; then echo --disable-debug; fi` \
       `if test "$cross_compiling" = "yes"; then echo --enable-cross-compile; fi` \
+      `if test -n "$host_alias"; then echo --cross-prefix="${host_alias}-"; fi`\
       `if test "$use_arch" != "no"; then echo --arch=$use_arch; fi`\
       `if test "$use_cpu" != "no"; then echo --cpu=$use_cpu; fi`\
       --target-os=$(tolower $(uname -s)) \
